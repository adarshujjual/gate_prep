#include <stdio.h>
#include <stdbool.h>

    int main(void){

         int i; // declare i variable of integer type
         int x=11;
         int y=0;
         i = 2; // Assign the value 2 into the variable i

            /*bool x = true;  // declared x as boolean variable and assign value as true

            //if (x) {
            //printf("x is true!\n");
         
         

         
            // This expression:
               y += x > 10? 17: 37;
            // is equivalent to this non-expression:
              if (x > 10)
              y += 17;
              else
              y += 37;*/ 

    printf("%d   and  %d \n",x,y);
    printf("The number %d is %s.\n", x, x % 2 == 0? "even": "odd");

    }
